[build]
builder = "dockerfile"
# Estrategia de LIQUIDEZ (Python). Para volver al monitor Rust: dockerfilePath = "Dockerfile"
dockerfilePath = "Dockerfile.liquidity"

[deploy]
restartPolicyType = "always"
region = "asia-southeast1"
